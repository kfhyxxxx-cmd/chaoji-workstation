# Infinite Canvas（无限画布）操作手册

> **版本：** 2026.08.04  
> **版本：** v2026.08.04  
> **许可证：** 个人/公司可使用，禁止商用，二次开发须开源并注明来源

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统要求](#2-系统要求)
3. [安装与启动](#3-安装与启动)
4. [界面导览](#4-界面导览)
5. [配置生成方式](#5-配置生成方式)
6. [普通无限画布使用指南](#6-普通无限画布使用指南)
7. [智能画布使用指南](#7-智能画布使用指南)
8. [素材库管理](#8-素材库管理)
9. [图片编辑功能](#9-图片编辑功能)
10. [视频生成](#10-视频生成)
11. [ComfyUI 工作流集成](#11-comfyui-工作流集成)
12. [循环节点与批量处理](#12-循环节点与批量处理)
13. [LLM 大语言模型节点](#13-llm-大语言模型节点)
14. [Chrome 采集插件与 PS 插件](#14-chrome-采集插件与-ps-插件)
15. [项目更新指南](#15-项目更新指南)
16. [常见问题排查](#16-常见问题排查)
17. [附录：目录结构说明](#17-附录目录结构说明)

---

## 1. 项目概述

### 1.1 这是什么

Infinite Canvas（无限画布）是一个**本地运行的 AI 创作工作台**，通过浏览器访问 `http://127.0.0.1:3000/`，在一个统一的网页界面中调用多种 AI 生成服务，进行图片、视频、文本的创作与编辑。

### 1.2 核心特性

| 特性 | 说明 |
|------|------|
| **多平台接入** | 支持 API 平台、ModelScope、RunningHub、本地 ComfyUI、火山引擎、即梦 CLI |
| **双模式画布** | 普通无限画布（节点式）+ 智能画布（卡片式），适应不同场景 |
| **图片生成** | 文生图、图生图、风格迁移、光线迁移、高清放大 |
| **视频生成** | 文生视频、图生视频、视频转视频、视频帧抽取 |
| **LLM 对话** | 文本对话、图片理解、提示词反推 |
| **图片编辑** | 裁剪、遮罩编辑、局部重绘、扩图（Outpaint）、多宫格切图 |
| **批量处理** | 循环节点支持批量生成、批量扩图、分镜批量处理 |
| **素材库** | 全局跨画布素材管理，支持角色/场景/参考图分类 |
| **实时同步** | WebSocket 多窗口实时同步，局域网多人协作 |
| **扩展工具** | Chrome 浏览器采集插件、Photoshop 直连插件 |

### 1.3 支持的平台一览

| 平台 | 用途 | 费用 |
|------|------|------|
| **API 平台**（APIMart 等） | 高质量图片/视频/LLM 模型 | 付费 |
| **ModelScope** | 免费图片/LLM/VL 模型 + LoRA | 免费（需实名） |
| **RunningHub** | 云端 ComfyUI 工作流/AI 应用 | 点数/余额 |
| **本地 ComfyUI** | 调用本机显卡运行工作流 | 免费（需显卡） |
| **火山引擎** | 图片/视频模型 | 按量付费 |
| **即梦 CLI** | 调用即梦高级会员积分 | 会员积分 |

---

## 2. 系统要求

### 2.1 Windows

- Windows 10/11（64 位）
- Python 3.10+（可选，网盘整包自带便携 Python）
- 建议路径：`D:\Infinite-Canvas\`（避免中文/空格/特殊符号）

### 2.2 macOS

- macOS 10.14 或更高版本
- Python 3.10+
- 建议路径：`/Users/你的用户名/Downloads/Infinite-Canvas/`

### 2.3 网络要求

- 打开网页本身不需要联网
- 调用 API / ModelScope / RunningHub 等云端服务需要联网
- 仅使用本地 ComfyUI 可离线运行

---

## 3. 安装与启动

### 3.1 Windows 启动（推荐网盘整包）

**方法一：一键启动**

1. 解压项目文件夹到 `D:\Infinite-Canvas\`
2. 双击 `run.bat`（或网盘版 `启动服务.bat`）
3. 黑色命令行窗口会显示访问地址，浏览器自动打开 `http://127.0.0.1:3000/`
4. **不要关闭命令行窗口**，关闭即停止服务

**方法二：缺少依赖时**

如果双击后报错 `ModuleNotFoundError`，先双击 `安装依赖.bat`，完成后再双击 `run.bat`。

**方法三：没有 python 文件夹**

1. 下载项目根目录中的 `python.zip`
2. 解压到项目目录，确保出现 `python\python.exe`
3. 再双击 `run.bat`

### 3.2 macOS 启动

1. **首次使用：** 右键点击 `mac-启动服务.command` → 选择「打开」→ 点击「打开」
2. **之后使用：** 直接双击 `mac-启动服务.command`
3. 如果提示「无法验证开发者」，先右键打开 `mac-修复权限.command`，再启动服务

也可在终端手动启动：
```bash
cd 项目文件夹
python3 main.py
```

### 3.3 局域网访问

启动后命令行会显示局域网地址，例如 `http://192.168.x.x:3000/`，同一网络下的其他设备可直接访问。若防火墙弹窗，请允许专用网络访问。

### 3.4 停止服务

在命令行窗口按 `Ctrl+C` 即可停止。

---

## 4. 界面导览

### 4.1 首页

打开 `http://127.0.0.1:3000/` 后，首页包含以下入口：

- **普通无限画布：** 节点式编辑器，适合复杂工作流
- **智能画布：** 卡片式创作工具，适合新手和灵感板
- **画布列表：** 管理所有画布（新建/删除/重命名）
- **API 设置：** 配置所有生成平台
- **素材库：** 管理图片/视频素材
- **ComfyUI 设置：** 配置本地 ComfyUI 和工作流

### 4.2 导航栏

- **GitHub 按钮：** 检查项目更新（依赖 `VERSION` 文件）
- **语言切换：** 中/英文
- **深浅色模式：** 切换主题

### 4.3 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| 鼠标拖拽空白区域 | 移动画布视野 |
| 滚轮 | 缩放画布 |
| `Ctrl+G` | 选中节点成组 |
| `Ctrl+Shift+G` | 取消分组 |
| `Z`（智能画布） | 全局预览所有节点 |
| `A`（智能画布） | 打开素材库 |
| `Ctrl+Shift+R` | 强制刷新（清除缓存） |

---

## 5. 配置生成方式

项目启动后，至少要配置一种生成方式才能使用。点击左下角「设置」或导航栏「API 设置」。

### 5.1 配置 API 平台（推荐新手）

**推荐平台：** APIMart（https://apimart.ai/register?aff=1uyAbb）

1. 注册并获取 API Key
2. 在 API 设置页面中填写：
   - **请求地址：** `https://api.apimart.ai`
   - **协议：** `APIMart 异步`
   - **API Key：** 粘贴你的密钥
3. 点击「验证地址」→「验证协议」→「拉取模型」
4. 勾选需要的图片模型、视频模型、LLM 模型
5. 保存

> **注意：** 账户需至少有 0.05 元余额才能正常验证。如果解析失败，可尝试备用地址：`api.apib.ai`、`api.aiuxu.com`、`api.aishuch.com`

### 5.2 配置 ModelScope（免费）

1. 注册 [ModelScope](https://modelscope.cn)，在账号设置中创建长期 Token
2. 在 API 设置中找到 ModelScope
3. 填入 Token → 点击验证 → 拉取模型
4. 勾选图片模型和 LLM 模型，保存

**ModelScope 支持：**
- 图片模型：Z-Image-Turbo、Qwen-Image、FLUX.2-Klein 等
- LLM 模型：Qwen3-235B、Qwen3-VL（视觉模型）、MiniMax-M2.7 等
- LoRA：可在设置中添加自定义 LoRA

### 5.3 配置 RunningHub

1. 在 [RunningHub](https://www.runninghub.ai) 获取 API Key
2. 在 API 设置中填入 Key
3. 添加 AI 应用或工作流：在 RunningHub 找到目标 → 点击 API → 复制信息 → 粘贴到项目中

### 5.4 配置本地 ComfyUI

1. 确保本地 ComfyUI 已启动并可访问（默认 `http://127.0.0.1:8188/`）
2. 在 ComfyUI 设置中填入后端地址
3. 可选：导入自定义工作流 JSON（需先在 ComfyUI 中导出 API 格式）

### 5.5 配置即梦 CLI

1. 双击 `安装即梦CLI.bat`（Mac：`安装即梦CLI.command`）
2. 双击 `登录即梦CLI.bat`（Mac：`登录即梦CLI.command`）
3. 按提示完成网页登录
4. 回到 API 设置，新增 API，协议下拉选择「即梦 CLI」，点击验证

---

## 6. 普通无限画布使用指南

### 6.1 基本概念

普通无限画布是**节点式编辑器**，将图片、提示词、生成节点、处理节点用连线串联成工作流。

### 6.2 常用节点类型

| 节点 | 功能 |
|------|------|
| 📷 **图片节点** | 上传/拖入/粘贴图片 |
| ✏️ **提示词节点** | 编写正向提示词，可调用提示词库 |
| 🎨 **API 生成节点** | 调用 API 平台生成图片/视频 |
| 🆓 **ModelScope 节点** | 调用 ModelScope 免费模型 |
| ⚙️ **ComfyUI 节点** | 调用本地/自定义工作流 |
| ☁️ **RunningHub 节点** | 调用云端工作流/AI 应用 |
| 🤖 **LLM 节点** | 调用大语言模型 |
| 🔄 **循环节点** | 批量生成/迭代处理 |
| 📤 **输出节点** | 收集结果，预览/下载 |

### 6.3 基本操作流程

1. **新建画布：** 画布列表 → 新建普通画布
2. **添加图片节点：** 拖入图片或点击上传
3. **添加提示词节点：** 编写描述文字
4. **添加生成节点：** 选择 API / ModelScope / ComfyUI 等
5. **连接节点：** 从图片节点拖线到生成节点的输入，从提示词节点拖线到生成节点
6. **运行：** 点击生成节点上的运行按钮
7. **查看结果：** 结果出现在输出端，可继续连接或双击预览

### 6.4 节点操作

- **拖线连接：** 从一个节点的输出端口拖到另一个节点的输入端口
- **从输出图拖出：** 自动创建新的图片节点
- **右键节点：** 打开快捷菜单（删除/复制/设置等）
- **框选 + Ctrl+G：** 成组
- **Ctrl+Shift+G：** 取消分组

---

## 7. 智能画布使用指南

### 7.1 基本概念

智能画布是**卡片式创作工具**，不需要手动连线。适合新手、灵感板、快速编辑。

### 7.2 基本操作

1. 新建智能画布
2. 拖入图片/视频或从素材库选择
3. 选择生成方式（API / ModelScope / ComfyUI / RunningHub）
4. 编写提示词
5. 点击运行

### 7.3 智能画布特色

- **编辑模式：** 看到哪张图就编辑哪张图，不需要手动连线
- **@ 引用素材：** 在提示词中输入 `@` 可快速引用素材库中的图片
- **循环节点：** 拖到目标卡片附近自动连接，设置循环次数和变化提示词即可批量生成
- **全局预览：** 按 `Z` 查看所有卡片结构

---

## 8. 素材库管理

### 8.1 打开素材库

- 普通画布/智能画布中点击「素材库/资产库」按钮
- 智能画布中按 `A` 快捷键

### 8.2 素材操作

- **上传：** 拖入图片/视频
- **分类：** 按角色、场景、参考图等建文件夹管理
- **重命名：** 右键素材修改名称
- **保存到素材库：** 从画布输出结果右键保存
- **跨画布复用：** 素材库是全局的，所有画布共享

### 8.3 在提示词中引用

在智能画布的提示词框中输入 `@`，弹出素材选择器，选择素材即可作为参考图输入。

---

## 9. 图片编辑功能

双击任意图片进入编辑模式，支持以下功能：

| 功能 | 说明 |
|------|------|
| **预览** | 缩放、拖动查看细节 |
| **对比** | 原图与结果图并排对比 |
| **裁剪** | 自由裁剪画面 |
| **遮罩编辑** | 涂抹区域，只让 AI 修改涂抹部分 |
| **画笔局部编辑** | 手动绘制修改区域 |
| **扩图（Outpaint）** | 向外扩展画面边界 |
| **网格切图** | 将九宫格/多宫格拆成单张 |
| **视频帧抽取** | 从视频中提取关键帧 |

---

## 10. 视频生成

### 10.1 支持的视频能力

- 文生视频
- 图生视频
- 视频转视频
- 视频帧抽取

### 10.2 重要注意

> ⚠️ **多数 API 平台不接受本地视频文件路径，需要公网可访问的 URL。**

- 使用视频节点时，先点击「上传云端」获取公网地址
- 也可使用自己的对象存储（阿里云 OSS 等）
- RunningHub 的 Seedance 等视频工作流可直接使用

---

## 11. ComfyUI 工作流集成

### 11.1 导入自定义工作流

1. 在 ComfyUI 中确认工作流能正常运行
2. 导出为 API 格式 JSON（Export API）
3. 回到项目 → ComfyUI 设置 → 上传工作流 JSON
4. 项目自动识别参数（图片输入、文本输入、LoRA、seed、分辨率等）
5. 勾选需要暴露给画布的输入，调整顺序
6. 在测试区上传测试图片和提示词，点击运行测试
7. 确认无误后保存

### 11.2 工作流输入顺序

- 如果工作流需要多张图（如原图 + 风格参考图），确认编号和顺序
- Mask 图建议转成黑白图片作为普通图片输入

### 11.3 内置工作流

项目 `workflows/` 目录提供预置工作流：
- Z-Image 图片生成
- Flux2-Klein 细节增强
- QwenEdit 图片编辑
- JoyCaption 反推提示词
- SeedVR2 高清放大
- LTXDirectorv2 视频生成

---

## 12. 循环节点与批量处理

### 12.1 适用场景

- 一次生成多个卖点图
- 对一组分镜逐张扩图
- 同一个人物批量生成不同表情
- 用同一工作流批量跑多张图

### 12.2 使用方法

1. 在普通画布中添加「循环节点」
2. 上游输入基础提示词
3. 下方写多个变化项（每行一个）
4. 循环节点每运行一次自动切换到下一个变化项
5. 设置循环次数或自动遍历所有变化项

### 12.3 智能画布中的循环

将循环节点拖到目标卡片附近，自动连接。设置循环次数，写多条变化提示词，点击运行。

---

## 13. LLM 大语言模型节点

### 13.1 功能

- 文本对话
- 图片理解（需选择 VL/视觉模型）
- 提示词反推（从图片生成描述文本）
- 文本翻译/润色

### 13.2 使用方式

1. 在画布中添加 LLM 节点
2. 选择 LLM 模型（如 Qwen3-235B、GPT-5.1、Gemini 等）
3. 输入文本提示词，或连接图片节点进行图片理解
4. 运行，结果以文本形式输出

---

## 14. Chrome 采集插件与 PS 插件

### 14.1 Chrome 采集插件

**安装地址：** [Chrome 网上应用店](https://chromewebstore.google.com/detail/infinite-canvas-%E5%9B%BE%E5%83%8F%E8%A7%86%E9%A2%91%E6%96%87%E5%AD%97%E6%8A%93%E5%8F%96%E5%B7%A5/ajfhnbklbmpfaaookhfakohabnpmlcic)

功能：浏览网页时一键采集图片/视频/文字到 Infinite Canvas 素材库。

本地版本在 `tools/chrome-local-asset-importer/` 目录中，可手动加载。

### 14.2 Photoshop 直连插件

位于 `tools/photoshop-asset-connector/` 目录。

功能：在 Photoshop 中直接调用 Infinite Canvas 的所有生成功能，无需切换窗口。

---

## 15. 项目更新指南

### 15.1 检查更新

导航栏 GitHub 按钮会对比本地 `VERSION` 文件与远程版本，有新版本时提示。

### 15.2 更新方法（推荐）

1. 下载最新整包
2. 解压到新文件夹
3. 将旧项目的个人数据复制到新项目：

```
旧项目 → 新项目 需要保留的目录：
├── API/           （API 配置）
├── data/          （画布数据、素材库）
├── assets/        （素材文件）
├── output/        （生成结果）
├── workflows/custom/ （自定义工作流）
```

> ⚠️ 不要直接用旧版本覆盖新版本，尤其是 `static/` 目录。

### 15.3 更新后清除缓存

更新后如果页面显示异常，按 `Ctrl+Shift+R` 强制刷新，或 `F12` → 右键刷新按钮 →「清空缓存并硬性重新加载」。

---

## 16. 常见问题排查

### 16.1 启动问题

| 问题 | 解决方案 |
|------|----------|
| 双击 bat 一闪而过 | 在项目文件夹 Shift+右键 →「在终端中打开」→ 输入 `.\run.bat` 查看错误 |
| 端口 3000 被占用 | 关闭之前的命令行窗口，或重启电脑 |
| 浏览器打不开 | 手动访问 `http://127.0.0.1:3000/` |
| 防火墙弹窗 | 允许专用网络访问 |
| `ModuleNotFoundError` | 运行 `安装依赖.bat` |

### 16.2 API 配置问题

| 问题 | 解决方案 |
|------|----------|
| 无法拉取模型 | 检查 Key 完整性、地址空格、账户余额、协议选择 |
| 生成报错 | 查看右上角日志，常见原因：余额不足、模型下架、审核不通过 |
| 视频生成提示无法上传本地文件 | 先点击「上传云端」获取公网 URL |

### 16.3 ModelScope 问题

- 需要在账号设置中绑定阿里云并实名认证
- 图片识别需选择 VL/视觉模型，普通文本模型不能看图

### 16.4 RunningHub 问题

- 区分点数消耗和余额消耗，部分模型必须用余额
- 工作流必须已发布才能通过 API 调用
- 复杂工作流建议先简化，删除无关节点

### 16.5 页面显示问题

- 更新后先 `Ctrl+Shift+R` 强制刷新
- 如果还不行，F12 → 右键刷新 →「清空缓存并硬性重新加载」
- 或换浏览器测试

### 16.6 完全不会排查

推荐安装 [Codex](https://openai.com/codex/) 或 [Trae](https://opencode.ai/)，打开项目文件夹后直接输入：

> 这个项目运行报错，请帮我修到能打开 http://127.0.0.1:3000/

---

## 17. 附录：目录结构说明

```
Infinite-Canvas/
├── main.py                    # 后端主程序（FastAPI 服务）
├── run.bat                    # Windows 启动脚本
├── 安装依赖.bat                # Windows 依赖安装脚本
├── requirements.txt           # Python 依赖清单
├── VERSION                    # 版本号文件（删除可关闭更新提示）
├── README.md                  # 项目说明
├── 新手运行与使用教程.md        # 详细新手教程
│
├── API/                       # API 密钥配置（.env 文件）
├── python/                    # 便携 Python 运行环境（网盘版）
├── packages/                  # 离线依赖包
│
├── static/                    # 前端页面
│   ├── index.html             # 首页
│   ├── canvas.html            # 普通无限画布
│   ├── smart-canvas.html      # 智能画布
│   ├── api-settings.html      # API 设置页面
│   ├── asset-manager.html     # 素材库
│   ├── canvas-list.html       # 画布列表
│   ├── comfyui-settings.html  # ComfyUI 设置
│   ├── gpt-chat.html          # LLM 对话
│   ├── enhance.html           # 图片增强
│   ├── angle.html             # 360 全景图
│   ├── klein.html             # FLUX Klein 模型
│   ├── online.html            # 在线服务
│   ├── zimage.html            # Z-Image 模型
│   ├── css/                   # 样式文件
│   ├── js/                    # JavaScript 脚本
│   └── images/                # 图片资源
│
├── data/                      # 用户数据
│   ├── api_providers.json     # API 平台配置
│   ├── projects.json          # 项目列表
│   ├── asset_library.json     # 素材库索引
│   ├── canvases/              # 画布数据
│   ├── conversations/         # 对话历史
│   ├── media_previews/        # 媒体预览缩略图
│   └── update_backups/        # 更新备份
│
├── assets/                    # 素材文件
│   ├── input/                 # 输入图片
│   ├── output/                # 生成结果
│   ├── library/               # 素材库文件
│   │   ├── 角色/              # 角色素材
│   │   └── 场景/              # 场景素材
│   └── uploads/               # 上传文件
│
├── workflows/                 # ComfyUI 工作流
│   ├── Z-Image.json
│   ├── Flux2-Klein.json
│   ├── upscale.json
│   └── custom/                # 自定义工作流
│
├── tools/                     # 扩展工具
│   ├── chrome-local-asset-importer/  # Chrome 采集插件
│   └── photoshop-asset-connector/    # PS 直连插件
│
├── CLI/                       # 各平台 CLI 安装脚本
│   ├── windows/               # Windows 脚本
│   ├── macos/                 # macOS 脚本
│   └── linux/                 # Linux 脚本
│
├── tests/                     # 测试文件
├── output/                    # 输出目录
└── logs/                      # 日志文件
```

---

## 快速上手路线（新手推荐）

1. ✅ 解压整包
2. ✅ Windows 双击 `run.bat` / Mac 右键打开 `mac-启动服务.command`
3. ✅ 浏览器打开 `http://127.0.0.1:3000/`
4. ✅ 配置 API（推荐 APIMart）或 ModelScope
5. ✅ 新建智能画布
6. ✅ 拖入一张图片，写一句提示词，点击运行
7. ✅ 学会保存素材到素材库
8. ✅ 再学习普通画布节点连接、循环节点、ComfyUI 等高级功能

> **一句话总结：解压 → 双击启动 → 打开网页 → 配置一种生成方式 → 开始创作！**

---

*本手册基于 Infinite Canvas v2026.08.04 编写，项目持续更新中。*